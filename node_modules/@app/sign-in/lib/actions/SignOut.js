import { App, Action } from "spak"

@Action.ns("SignOut")
export default class SignOut extends Action {
    exec({ presenter }) {
        try {
            App.session().isAuthenticated = false;
            presenter.showSignInScreen();
        } catch (ex) {
            console.error(ex);
        }
    }
}
