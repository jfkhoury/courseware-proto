import { App, Action } from "spak"

@Action.ns("SignIn")
export default class SignIn extends Action {
    constructor(stores) {
        super(stores);
        this._stores = stores;
    }

    exec({ presenter }) {
        App.session().isAuthenticated = true;
        this._stores.loadAll();
        presenter.showAuthenticatedScreen();
    }
}
